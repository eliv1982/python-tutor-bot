"""FastAPI web/API delivery adapter (Stage 6A) — alongside handlers/ (the
Telegram adapter). Resolves authenticated users through the canonical
identity/application layer (app/auth_session.py, app/identity.py) and
contains no business logic of its own."""
